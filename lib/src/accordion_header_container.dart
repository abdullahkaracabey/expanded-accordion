import 'package:flutter/widgets.dart';

class AccordionHeaderContainer {
  final Widget title;
  final Color? color;
  final Widget? icon;

  AccordionHeaderContainer({required this.title, this.icon, this.color});
}
